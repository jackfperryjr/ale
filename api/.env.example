# Copy to .env — never commit .env to git

# Neon Postgres connection string
# Find in: Neon console → your project → Connection Details → "Connection string"
DATABASE_URL=postgresql://user:pass@ep-something.region.aws.neon.tech/dbname?sslmode=require

# Hive AI — the "secret key" from your Hive dashboard
HIVE_SECRET_KEY=
